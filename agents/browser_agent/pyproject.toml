[project]
name = "browser-agent"
version = "0.1.0"
description = "Local browser-use agent for testing pkmn-rse-browser end-to-end in a real browser"
requires-python = ">=3.11"
dependencies = [
    "browser-use>=0.2.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
